# NPM configuration
# legacy-peer-deps: Skip peer dependency version conflicts
legacy-peer-deps=true

# include: Force installation of devDependencies even when NODE_ENV=production
# Required for Railway builds because @nestjs/cli (in devDependencies) is needed for 'nest build'
include=dev
